[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    requires_pybamm: requires PyBaMM environment
    requires_torch: requires PyTorch
addopts = -v --tb=short --strict-markers
